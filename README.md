# How to run the project

Open a terminal in the server directory and type "node server.js"

Open another terminal in the client directory and type "npm install" and then "npm run dev"

The application is now live on http://localhost:5173/
