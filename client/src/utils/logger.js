/**
 *
 * @param error An error object
 */
const logErrors = (error) => {
    // Here we can do whatever with the error object
    //  - check for messages
    //  - write to a file not the browser
    //  - etc.

    console.log(error);
};

export default logErrors;
